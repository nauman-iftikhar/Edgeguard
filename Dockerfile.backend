FROM python:3.11-slim

WORKDIR /app

RUN pip install flask flask-cors minio requests --break-system-packages 2>/dev/null || \
    pip install flask flask-cors minio requests

COPY backend.py .

RUN mkdir -p snapshots

EXPOSE 8000

CMD ["python", "backend.py"]
